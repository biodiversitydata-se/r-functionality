.onAttach <- function(libname, pkgname) {
    packageStartupMessage("*NOTE, May 2017* --- some field (column) names in returned data objects may have changed with recent changes to the NBN Atlas infrastructure. These are liable to remain in flux but are being investigated.")
}
